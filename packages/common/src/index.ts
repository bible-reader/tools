import * as types from "./types";
import config from "./config";

export type BookNumbers = types.BookNumbers;
export type BookStats = types.BookStats;
export type Passage = types.Passage;

export { config };
